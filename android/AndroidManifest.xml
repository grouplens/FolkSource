<?xml version="1.0" encoding="utf-8"?>
<!--
   Copyright (c) 2006-2011 Regents of the University of Minnesota.
   For licensing terms, see the file LICENSE.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.citizensense.android"
    android:versionCode="1"
    android:versionName="0.1.0-beta" >

    <uses-sdk
        android:minSdkVersion="4"
        android:targetSdkVersion="4" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" >
    </uses-permission>

    <application
        android:debuggable="true"
        android:icon="@drawable/temp_icon"
        android:label="@string/app_name"
        android:name="Init" >
        <activity
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:name=".CitizenSense"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="CampaignBrowser"
            android:name=".CampaignBrowser" />
        <activity
            android:label="MyCampaigns"
            android:name=".MyCampaigns" />
        <activity
            android:label="GalleryCampaign"
            android:name=".CampaignGallery" />
        <activity
            android:label="Home"
            android:name=".Home" />
        <activity
            android:label="Map"
            android:name=".Map" />
        <activity
            android:label="Profile"
            android:name=".Profile" />
        <activity
            android:label="Sense"
            android:name=".Sense" />
        <activity
            android:label="SubmissionHistory"
            android:name=".SubmissionHistory" />
        <activity
            android:label="Login"
            android:name=".Login"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:label="Register"
            android:name=".Register"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:label="CampaignPage"
            android:name=".CampaignPage" />
        <activity
            android:label="ForgetPassword"
            android:name=".ForgetPassword" />

        <uses-library android:name="com.google.android.maps" >
        </uses-library>
    </application>

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" >
    </uses-permission>
    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" >
    </uses-permission>
    <uses-permission android:name="android.permission.CAMERA" >
    </uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" >
    </uses-permission>

    <uses-feature android:name="android.hardware.camera" />

</manifest>